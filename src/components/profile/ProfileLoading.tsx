import Animated, { Easing, useAnimatedStyle, useSharedValue, withRepeat, withTiming } from 'react-native-reanimated';
import { useEffect } from 'react';
import { StyleSheet, View } from 'react-native';
import { Radius, Spacing, profileColors } from '@/constants/theme';
export function ProfileLoading() { const opacity = useSharedValue(0.45); useEffect(() => { opacity.value = withRepeat(withTiming(1, { duration: 850, easing: Easing.inOut(Easing.ease) }), -1, true); }, [opacity]); const animated = useAnimatedStyle(() => ({ opacity: opacity.value })); return <View style={styles.container}><Animated.View style={[styles.header, animated]} /><Animated.View style={[styles.card, animated]} /><View style={styles.grid}><Animated.View style={[styles.stat, animated]} /><Animated.View style={[styles.stat, animated]} /><Animated.View style={[styles.stat, animated]} /><Animated.View style={[styles.stat, animated]} /></View><Animated.View style={[styles.card, animated]} /></View>; }
const styles = StyleSheet.create({ container: { gap: Spacing.four }, header: { backgroundColor: profileColors.tint, borderRadius: Radius.large, height: 310 }, card: { backgroundColor: profileColors.tint, borderRadius: Radius.large, height: 250 }, grid: { flexDirection: 'row', flexWrap: 'wrap', gap: Spacing.two, justifyContent: 'space-between' }, stat: { backgroundColor: profileColors.tint, borderRadius: Radius.medium, height: 130, width: '48%' } });
