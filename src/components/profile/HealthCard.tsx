import type { LucideIcon } from 'lucide-react-native';
import { Activity } from 'lucide-react-native';
import { StyleSheet, Text, View } from 'react-native';
import { Fonts, Radius, Spacing, brandColors, profileColors } from '@/constants/theme';
type HealthMetric = { label: string; value: string; icon: LucideIcon };
type HealthCardProps = { metrics: HealthMetric[] };
export function HealthCard({ metrics }: HealthCardProps) { return <View style={styles.card}><View style={styles.heading}><View style={styles.headingIcon}><Activity color={brandColors.primary} size={18}/></View><View><Text style={styles.title}>Health snapshot</Text><Text style={styles.description}>Your care essentials at a glance</Text></View></View><View style={styles.grid}>{metrics.map(({ icon: Icon, label, value }) => <View key={label} style={styles.metric}><View style={styles.metricIcon}><Icon color={brandColors.primaryMuted} size={16}/></View><Text style={styles.value} numberOfLines={1}>{value}</Text><Text style={styles.label}>{label}</Text></View>)}</View></View>; }
const styles = StyleSheet.create({ card: { backgroundColor: profileColors.card, borderRadius: Radius.large, gap: Spacing.three, padding: Spacing.four, shadowColor: profileColors.shadow, shadowOffset: { width: 0, height: 8 }, shadowOpacity: 0.06, shadowRadius: 22 }, heading: { alignItems: 'center', flexDirection: 'row', gap: Spacing.two }, headingIcon: { alignItems: 'center', backgroundColor: profileColors.tint, borderRadius: Radius.pill, height: 38, justifyContent: 'center', width: 38 }, title: { color: brandColors.primaryDark, fontFamily: Fonts.rounded, fontSize: 18, fontWeight: '800' }, description: { color: brandColors.slate, fontFamily: Fonts.sans, fontSize: 13, marginTop: 2 }, grid: { flexDirection: 'row', flexWrap: 'wrap', rowGap: Spacing.three }, metric: { gap: 3, width: '33.333%' }, metricIcon: { alignItems: 'center', backgroundColor: profileColors.sky, borderRadius: Radius.pill, height: 30, justifyContent: 'center', width: 30 }, value: { color: brandColors.primaryDark, fontFamily: Fonts.rounded, fontSize: 16, fontWeight: '800' }, label: { color: brandColors.slate, fontFamily: Fonts.sans, fontSize: 11, fontWeight: '600' } });
