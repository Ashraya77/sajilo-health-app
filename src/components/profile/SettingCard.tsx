import { ChevronRight } from 'lucide-react-native';
import type { LucideIcon } from 'lucide-react-native';
import { Pressable, StyleSheet, Text, View } from 'react-native';
import { Fonts, Radius, Spacing, brandColors, profileColors } from '@/constants/theme';
type SettingCardProps = { icon: LucideIcon; title: string; subtitle: string; onPress?: () => void; tone?: 'default' | 'danger' };
export function SettingCard({ icon: Icon, title, subtitle, onPress, tone = 'default' }: SettingCardProps) { const danger = tone === 'danger'; return <Pressable onPress={onPress} style={({ pressed }) => [styles.card, pressed && styles.pressed]}><View style={[styles.icon, danger && styles.dangerIcon]}><Icon color={danger ? profileColors.danger : brandColors.primary} size={20}/></View><View style={styles.copy}><Text style={[styles.title, danger && styles.dangerText]}>{title}</Text><Text style={styles.subtitle}>{subtitle}</Text></View><ChevronRight color={brandColors.softBlue} size={20}/></Pressable>; }
const styles = StyleSheet.create({ card: { alignItems: 'center', backgroundColor: profileColors.card, borderRadius: Radius.medium, flexDirection: 'row', gap: Spacing.two, minHeight: 78, padding: Spacing.three, shadowColor: profileColors.shadow, shadowOffset: { width: 0, height: 5 }, shadowOpacity: 0.045, shadowRadius: 13 }, pressed: { opacity: 0.72 }, icon: { alignItems: 'center', backgroundColor: profileColors.tint, borderRadius: Radius.small, height: 42, justifyContent: 'center', width: 42 }, dangerIcon: { backgroundColor: profileColors.dangerSurface }, copy: { flex: 1, gap: 3 }, title: { color: brandColors.primaryDark, fontFamily: Fonts.sans, fontSize: 15, fontWeight: '800' }, dangerText: { color: profileColors.danger }, subtitle: { color: brandColors.slate, fontFamily: Fonts.sans, fontSize: 12, fontWeight: '500' } });
