import { DestinationScreenShell } from '@/components';

// TODO: Implement the clinic-scoped diagnostic list after clinic selection is integrated.
export default function DiagnosticsRoute() {
  return <DestinationScreenShell title="Diagnostics" />;
}
