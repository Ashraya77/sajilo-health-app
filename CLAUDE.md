# Sajilo Health monorepo

- Use npm workspaces from the repository root.
- The Expo application lives in `apps/mobile`.
- The Next.js dashboard lives in `apps/web`.
- Framework-neutral code belongs in `packages/*`.
- Preserve existing mobile behavior and its `@/*` alias when changing shared code.
